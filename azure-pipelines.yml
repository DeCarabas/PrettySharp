jobs:
- job: linux
  pool:
    vmImage: 'ubuntu-16.04'
  steps:
  - script: make test
  - task: CopyFiles@2
    inputs:
      targetFolder: '$(Build.ArtifactStagingDirectory)'
      contents: 'prettysharp'
  - task: PublishBuildArtifacts@1
    inputs:
      pathtoPublish: '$(Build.ArtifactStagingDirectory)'
      artifactName: 'ubuntu-binary'

- job: macos
  pool:
    vmImage: 'macos-10.13'
  steps:
  - script: make test
  - task: CopyFiles@2
    inputs:
      targetFolder: '$(Build.ArtifactStagingDirectory)'
      contents: 'prettysharp'
  - task: PublishBuildArtifacts@1
    inputs:
      pathtoPublish: '$(Build.ArtifactStagingDirectory)'
      artifactName: 'macos-binary'

- job: windows
  pool:
    vmImage: 'vs2017-win2016'
  steps:
  - script: build.bat
    displayName: Build
  - script: python.exe test.py
    displayName: Test
  - task: CopyFiles@2
    inputs:
      targetFolder: '$(Build.ArtifactStagingDirectory)'
      contents: 'prettysharp.exe'
  - task: PublishBuildArtifacts@1
    inputs:
      pathtoPublish: '$(Build.ArtifactStagingDirectory)'
      artifactName: 'windows-binary'
